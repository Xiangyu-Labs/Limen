name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run build
        env:
          AUTH_PASSWORD: ci-placeholder-not-used-during-build
          DATABASE_URL: postgresql://test:test@localhost:5432/limen_ci
      - run: npm audit --audit-level=moderate
